- 
  title: Default bulid
  dir: test-site
  build: true
  compare: _expected _site
- 
  title: Dark theme
  dir: test-site
  build: true
  compare: _expected-dark _site/stylesheets
  config:
    littlefoot: _dark.yml
- 
  title: Custom theme
  dir: test-site
  build: true
  compare: _expected-custom _site/stylesheets
  config:
    littlefoot: _custom.yml
